#include <stdio.h>
#include "errors.h"

void updateErrorLog(Error err);
{
    switch (err)
    {
    case dupMacro:
    {
        break;
    }
    case syntaxErr:
    {
        break;
    }

    default:
        break;
    }
}